import { DocHandle } from "./api";

export const BranchPicker = (props: { handle: DocHandle<unknown> }) => {
  return null;
};

export const Timeline = (props: {
  handle: DocHandle<unknown>;
  enableRangeSelection?: boolean;
}) => {
  return null;
};
